`HEAD (pointer to the currently checked-out commit)`

`HEAD~<num> (relative ref - goes back <num> parents from HEAD)`

`HEAD^ (relative ref - goes back 1 parent, ^2 selects 2nd parent of a merge)`

`o/main, o/master (local reference to the remote's branch as last fetched)`

`--- LGB CONSOLE HELPERS (not real git, site-only) ---`

`show commands (lists commands unlocked at the current level)`

`level <name> (jumps directly to a specific level)`

`help level (re-displays the current level's goal/instructions)`

`reset (restarts the current level's tree from scratch)`