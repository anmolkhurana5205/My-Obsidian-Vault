`git pull (fetch + merge remote branch into current one)`

`git pull --rebase (fetch + rebase current branch onto remote branch)`

`git pull <remote> <source>:<destination> (pulls a remote branch into a chosen local branch)`

`git fetch; git merge o/main (manual equivalent of git pull)`

`git fetch; git rebase o/main (manual equivalent of git pull --rebase)`
