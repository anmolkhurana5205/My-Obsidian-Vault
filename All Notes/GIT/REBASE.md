`git rebase <branch> (moves current branch's commits onto another branch)`

`git rebase <branch1> <branch2> (replays branch1 onto branch2)
`
`git rebase -i <commit> (interactively reorder/drop/edit commits)`
