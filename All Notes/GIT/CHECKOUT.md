`git checkout <name> (switches HEAD to a branch)`

`git checkout <commit_hash> (switches HEAD to a specific commit, detached)`

`git checkout -b <name> (creates a new branch and switches to it)`

`git checkout -b <branch> o/main (creates local branch tracking a remote branch)`
