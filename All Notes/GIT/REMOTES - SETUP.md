`git clone (copies a remote repo, creates a local + origin)`

`git remote add <remote_name> <location> (links a remote repo under a given name)`

`git remote add origin <location> (links a remote repo under the name "origin")`
