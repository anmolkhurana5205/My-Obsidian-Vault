`git branch <name> (creates a new branch pointing at current commit)`

`git branch -f <branch> <commit> (force-moves a branch to point at a commit)
`
`git branch -f <branch> <relative_ref> (force-moves a branch using ~/^ relative refs)`

`git branch -u <remote>/<branch> <branch> (sets/changes which remote branch is tracked)`

