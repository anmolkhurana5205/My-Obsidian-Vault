`git push (uploads local commits to the tracked remote branch)`

`git push <remote> <branch> (uploads a specific branch to a remote)`

`git push <remote> <source>:<destination> (pushes local branch to a differently-named remote branch)`

`git push <remote> :<branch> (deletes a branch on the remote)`
