avalanche, polygon, phantom, harmony
- **ABI** - Application Binary Interface (When Solidity code is compiled, it becomes **bytecode (machine code)** which humans can’t read.  ABI is the **JSON format** that describes how to call functions in that bytecode.)
- **block** - a list of transactions that mined together.
- **Contract** - It is similar to class in other programming language. 
- **consensus** - the mechanism used to agree on the state of block chain.
	- chain selection
	- sybil resistance 
- **Composability** - The ability smart contract to seamlessly communicate each other is called composability.
compile time
digital store of value
decentralized transactions
deployment time
decentralized agreements
decentralized organizations
defi (decentralized finance)
dapp (decentralized application)
DAOs (decentralized autonomous organizations)
decentralized oracle network (chain link)
- **deterministic nature of blockchain** - means it behaves like a strict mathematical system — the same input always gives the same output, ensuring all nodes stay synchronized.
double Spending Problem
gas
genesis block
gwei, wei
hybrid smart contracts
- **hash algorithm** - a function that computes data into a unique hash.
Layer 1 block chain (bitcoin, ethereum and avalanche)
Layer 2 block chain - any blockchain built on top of layer 1 ()
metamask
- **mining** - the  process of finding the problem of a blockchain problem, in our example the problem was to find a hash that starts with the four zeros. (nodes get paid for mining blocks)
- Mempool - Temporary holding space for unconfirmed transactions
NFTs (non fungible tokens)
- **node** - a single instance of a decentralized network.
node operators
- **nonce** - a number used once to find the solution to the blockchain problem.
- **oracle** - it is basically a device or entity that connects the deterministic blockchain to off chain data. (oracle is known as blockchain middleware)
- **pseudo anonymous** - in any transaction only wallet id is visible and not any sensitive information)
- **public key** - it is derived from the private key anyone can see it and use it to verify that the transaction is came from you.
- PoW - 
- PoS - 
Roll ups - solution of scalability issue.
Side chains
smart contracts
speed and efficiency
security and immutability
- **signing the transaction** - A "one way" process. Someone with a private key signs a transaction by their private key being hashed with their transaction data. Anyone can then verify this new transaction hash with your public key.
sybil attack - it is prevented by PoW and PoS
Scalability Problem - 
sharding - it is one of the solution of scalability issue.
- **oracle problem** - as we know that blockchain themselves cannot access the off chain data to maintain its deterministic nature so for that purpose we need a entity that pushes the off chain data into the blockchain so that all the nodes can reach to consensus and remain synchronized but due to this our whole chain depends on the centralized entity and this makes the whole execution of the smart contract centralized so that's a oracle problem.
transparency and flexibility
51% attack - 
Note - the more people use a chain, the more expensive it is to send transaction.
